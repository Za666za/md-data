7day



- template 温度范围[0-1], 越小越确定

- prompt 增强,根据任务类型,提供不同要求, 如判对错

- 具体怎么切换不同场景使用呢?  通过创建模型类,调用方法传参 

- -

- lora微调 用在是输出更稳定持久情况

  - 典型应用在需要强风格化时

  - ```
    
    server:
      port: 7091
    
    spring:
      application:
        name: seata-server
      logging:
        config: classpath:logback-spring.xml
        file:
        	path: ${log.homeL${user.home}/logs/seata}
      console:
        user:
          username: seata
          password: seata
    
    # ======= 下面是 Seata 事务核心配置，你提供的片段里没有包含 =======
    seata:
      config:
        type: nacos # 配置中心类型
        nacos:
          server-addr: 127.0.0.1:8848
          namespace: 641af599-574b-4849-aa64-61b6278dd2b8
          group: DEFAULT_GROUP
          data-id: seata-server.properties
      registry:
        type: nacos # 注册中心类型
        nacos:
          appilcation: seata-server
          server-addr: 127.0.0.1:8848
          group: DEFAULT_GROUP
          namespace: 641af599-574b-4849-aa64-61b6278dd2b8
      security:
        secretKey: SeataSecretKey0c382ef121d778043159209298fd40bf3850a017
        tokenValidityInMilliseconds: 1800000
        ignore:
          urls: /,/**/*.css,/**/*.js,/**/*.html,/**/*.map,/**/*.svg,/**/*.png,/**/*.jpeg,/**/*.ico,/api/v1/auth/login
    
    ```

  - 







































#  8day

# 9 day

